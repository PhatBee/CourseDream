import express from "express";
import { /* các handler nếu có, ví dụ getAllCategories */ } from "./category.controller.js";

const router = express.Router();

// ví dụ route
// router.get("/", getAllCategories);

export default router;