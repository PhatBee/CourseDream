# ===================================
# PAYMENT GATEWAY CONFIGURATION
# ===================================

# --- VNPAY Configuration ---
VNPAY_TMNCODE=your_vnpay_tmncode
VNPAY_HASHSECRET=your_vnpay_hashsecret
VNPAY_URL=https://sandbox.vnpayment.vn/paymentv2/vpcpay.html
VNPAY_RETURN_URL=http://localhost:5000/api/payment/vnpay_return

# --- MOMO Configuration ---
MOMO_PARTNER_CODE=your_momo_partner_code
MOMO_ACCESS_KEY=your_momo_access_key
MOMO_SECRET_KEY=your_momo_secret_key
MOMO_ENDPOINT=https://test-payment.momo.vn/v2/gateway/api/create
MOMO_RETURN_URL=http://localhost:5000/api/payment/momo_return
MOMO_IPN_URL=http://localhost:5000/api/payment/momo_ipn

# --- ZALOPAY Configuration ---
ZALOPAY_APP_ID=your_zalopay_app_id
ZALOPAY_KEY1=your_zalopay_key1
ZALOPAY_KEY2=your_zalopay_key2
ZALOPAY_ENDPOINT=https://sb-openapi.zalopay.vn/v2/create
ZALOPAY_QUERY_ENDPOINT=https://sb-openapi.zalopay.vn/v2/query
ZALOPAY_RETURN_URL=http://localhost:5000/api/payment/zalopay_return

# --- CLIENT RETURN URLs ---
# These are where the backend redirects after processing payment
WEB_RETURN_URL=http://localhost:3000/payment-return
MOBILE_RETURN_URL=http://localhost:3000/payment-return

# ===================================
# NOTES:
# ===================================
# 1. VNPAY_RETURN_URL, MOMO_RETURN_URL, ZALOPAY_RETURN_URL:
#    - These are backend endpoints that receive payment gateway callbacks
#    - Format: http://your-backend-domain/api/payment/{gateway}_return
#
# 2. WEB_RETURN_URL, MOBILE_RETURN_URL:
#    - These are client URLs where backend redirects after processing
#    - For web: typically your React app's payment result page
#    - For mobile: can be the same as web or a deep link scheme
#
# 3. In production:
#    - Replace localhost with your actual domain
#    - Use HTTPS for all URLs
#    - Ensure MOBILE_RETURN_URL is accessible from mobile devices
